export type Tokens = {
    access_token: string;
  };


/* 
El tipo "Tokens" define la estructura de los tokens de acceso utilizados en tu aplicación, con una propiedad 
llamada access_token que representa el token de acceso en sí mismo.
Esta propiedad recibirá el token en un formato String, ya que contiene una mezcla de caracteres y numeros
*/